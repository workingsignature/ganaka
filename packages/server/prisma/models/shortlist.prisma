model Shortlist {
    id String @id @default(uuid())

    instruments Instrument[]
    forDate     DateTime
    createdBy   User         @relation(fields: [createdById], references: [id])

    createdById String
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    @@index([createdById, forDate])
}
